package com.mj.mysns.post;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DefaultPostServiceTest {
//
//    @MockBean
//    PostRepository postRepository;
//
//    @Autowired
//    PostService postService;

    @Test
    void createPost_validPost_shouldSucceed() {
//        CreatePostDto validPost = new CreatePostDto("올바른 컨텐츠", null);
//        Post postToCreate = Post.create(null, validPost.content());
//        when(postRepository.save(any(Post.class)))
//            .thenReturn(postToCreate);
//
//        CreatePostDto createdPost = postService.createPost(validPost);
//
//        assertNotNull(createdPost);
//        assertEquals(createdPost.content(), validPost.content());
    }
}